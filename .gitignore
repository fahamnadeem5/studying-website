# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (keep the example tracked)
.env*
!.env.example

# local tooling (portable node downloaded into the workspace)
/.tools/

# freebuff thread state (logs, run doc, preview metadata)
/.freebuff/

# sqlite sidecar files (the catalog DB itself is committed)
/data/*.db-wal
/data/*.db-shm
/data/*.db-journal

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
